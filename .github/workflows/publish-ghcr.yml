name: Legacy publisher retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain the release path
        run: |
          echo "This legacy publisher is retired. Create a vMAJOR.MINOR.PATCH tag to use the verified ishiku release workflow."
